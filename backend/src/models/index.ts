// Re-export all schema types and tables for easier imports
export * from './schema.js';

// Database queries will be organized in separate files:
// - userQueries.ts (for user CRUD operations)
// - subscriptionQueries.ts (for subscription CRUD operations)
